# 🤖 Neo Worker - Опростен Browser Agent

## Какво е това?

**Един файл** вместо 15. Браузърът е **винаги отворен**. Комуникация в **реално време**.

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                         NEO AGENT                                │
│                    (говори с клиента)                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ WebSocket / HTTP
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                       NEO WORKER                                 │
│                    (Render Server)                               │
│                                                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                  PERSISTENT BROWSER                      │   │
│   │                  (винаги отворен)                        │   │
│   │                                                          │   │
│   │   Команди:                                               │   │
│   │   • open(url)     → отвори сайт                          │   │
│   │   • look()        → кажи какво виждаш                    │   │
│   │   • click(target) → кликни върху нещо                    │   │
│   │   • fill(target, value) → попълни поле                   │   │
│   │   • submit()      → изпрати формата                      │   │
│   │   • screenshot()  → направи снимка                       │   │
│   └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## Разликата

### ПРЕДИ (бавно) ❌
```
Заявка → Пали браузър (2s) → Работи (3s) → Затваря → Отговор
         ═══════════════════════════════════════════
                        ~5-10 секунди
```

### СЕГА (бързо) ✅
```
Заявка → Браузърът вече е отворен → Работи → Отговор
         ══════════════════════════════════
                    ~0.5-2 секунди
```

---

## Как работи (пример)

### NEO иска да провери наличност в хотел:

```
NEO: "Отвори https://hotel.bg/booking"
Worker: ✅ "Отворих. Виждам календар и бутон 'Провери наличност'"

NEO: "Кликни на 'Провери наличност'"  
Worker: ✅ "Кликнах. Сега виждам 3 свободни стаи с цени"

NEO: "Какви са цените?"
Worker: ✅ "Двойна стая: 120лв, Апартамент: 200лв, Лукс: 350лв"
```

**Общо време: ~3 секунди** (вместо 10+)

---

## API Reference

### HTTP Endpoint

```
POST /command
Content-Type: application/json
```

#### Отвори сайт
```json
{
  "action": "open",
  "url": "https://example.com"
}
```

#### Виж какво има
```json
{
  "action": "look"
}
```

Отговор:
```json
{
  "success": true,
  "message": "Виждам страница: https://example.com\n🔘 Бутони: \"Резервирай\", \"Контакти\"\n📝 Полета: email, phone",
  "data": {
    "buttons": [
      { "text": "Резервирай", "selector": "#book-btn" },
      { "text": "Контакти", "selector": "a.contact" }
    ],
    "inputs": [
      { "type": "email", "name": "email", "placeholder": "Вашият имейл" }
    ]
  }
}
```

#### Кликни
```json
{
  "action": "click",
  "target": "Резервирай"
}
```
или
```json
{
  "action": "click", 
  "target": "#book-btn"
}
```

#### Попълни поле
```json
{
  "action": "fill",
  "target": "email",
  "value": "ivan@mail.bg"
}
```

#### Изпрати форма
```json
{
  "action": "submit"
}
```

---

## WebSocket (за real-time)

```javascript
const ws = new WebSocket("wss://neo-worker.onrender.com");

ws.onopen = () => {
  // Изпрати команда
  ws.send(JSON.stringify({ action: "open", url: "https://hotel.bg" }));
};

ws.onmessage = (event) => {
  const result = JSON.parse(event.data);
  console.log(result.message);
  
  // Веднага изпрати следваща команда
  ws.send(JSON.stringify({ action: "look" }));
};
```

---

## Интеграция с execute-action (Supabase)

### Нов формат (препоръчителен):
```javascript
// От neo-agent-core
await fetch("/functions/v1/execute-action", {
  method: "POST",
  body: JSON.stringify({
    command: "open",
    url: "https://hotel.bg/booking"
  })
});

// После
await fetch("/functions/v1/execute-action", {
  method: "POST", 
  body: JSON.stringify({
    command: "look"
  })
});

// После
await fetch("/functions/v1/execute-action", {
  method: "POST",
  body: JSON.stringify({
    command: "click",
    target: "Резервирай"
  })
});
```

### Стар формат (обратна съвместимост):
```javascript
// Все още работи
await fetch("/functions/v1/execute-action", {
  method: "POST",
  body: JSON.stringify({
    type: "autoAvailability",
    payload: { url: "https://hotel.bg" }
  })
});
```

---

## Deploy на Render

1. Push към GitHub
2. Render → New Web Service
3. Connect repo
4. Settings:
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`
   - Environment: `PORT=3000`

---

## Файлова структура

```
neo-worker/
├── src/
│   └── worker.ts      ← ЕДИНСТВЕНИЯТ ФАЙЛ
├── supabase/
│   └── execute-action.ts   ← Supabase Edge Function
├── package.json
├── tsconfig.json
├── Dockerfile
└── README.md
```

**1 файл вместо 15. Това е всичко.**
