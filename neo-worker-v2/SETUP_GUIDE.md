# 🚀 NEO WORKER v2 - ПЪЛЕН SETUP GUIDE

## 📋 Какво ще направиш:

1. ✅ Deploy NEO Worker на Render (5-10 мин)
2. ✅ Добавиш secrets в Supabase/Lovable (2 мин)
3. ✅ Deploy execute-action функцията (2 мин)
4. ✅ Тестваш (2 мин)

---

# 🔐 ВСИЧКИ ENVIRONMENT VARIABLES

## 📦 RENDER (Neo Worker)

Когато създаваш Web Service в Render, добави тези ENV variables:

| Variable | Value | Описание |
|----------|-------|----------|
| `PORT` | `3000` | Порт на сървъра |
| `NEO_WORKER_SECRET` | `neo-secret-abc123xyz` | Парола за защита (измисли си сложна!) |

**Пример за NEO_WORKER_SECRET:**
```
NEO_WORKER_SECRET=neo-prod-8f4k2m9x7v3b1n5p
```

> ⚠️ **ВАЖНО:** Запомни тази парола - ще я сложиш и в Supabase!

---

## 🗄️ SUPABASE / LOVABLE (Secrets)

В Supabase Dashboard → Settings → Edge Functions → Secrets, добави:

| Secret Name | Value | Описание |
|-------------|-------|----------|
| `NEO_WORKER_URL` | `https://твоя-app.onrender.com` | URL на твоя Worker от Render |
| `NEO_WORKER_SECRET` | `neo-secret-abc123xyz` | **Същата парола** като в Render! |

**Пример:**
```
NEO_WORKER_URL=https://neo-worker-abc123.onrender.com
NEO_WORKER_SECRET=neo-prod-8f4k2m9x7v3b1n5p
```

> Вече имаш автоматично: `SUPABASE_URL` и `SUPABASE_SERVICE_ROLE_KEY`

---

# 📝 СТЪПКА ПО СТЪПКА

## Стъпка 1: Качи кода в GitHub

### 1.1 Създай ново repository в GitHub
- Отиди на https://github.com/new
- Name: `neo-worker`
- Visibility: Private (препоръчително)
- Create repository

### 1.2 Качи файловете
```bash
# Разархивирай neo-worker-v2.zip
cd neo-worker-v2

# Инициализирай git
git init
git add .
git commit -m "NEO Worker v2 - initial commit"

# Свържи с GitHub
git branch -M main
git remote add origin https://github.com/ТВОЕТО_ИМЕ/neo-worker.git
git push -u origin main
```

---

## Стъпка 2: Deploy на Render

### 2.1 Създай акаунт в Render
- Отиди на https://render.com
- Sign up (може с GitHub)

### 2.2 Създай Web Service
1. Dashboard → **New +** → **Web Service**
2. **Connect repository** → избери `neo-worker`
3. Настройки:

```
┌─────────────────────────────────────────────────────────────┐
│ Name:             neo-worker                                │
│ Region:           Frankfurt (EU Central)                    │
│ Branch:           main                                      │
│ Root Directory:   (празно)                                  │
│ Runtime:          Docker                                    │
│ Instance Type:    Free (или Starter $7/mo за production)   │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 Добави Environment Variables

Scroll надолу до **Environment Variables** и добави:

```
┌──────────────────────┬─────────────────────────────────┐
│ Key                  │ Value                           │
├──────────────────────┼─────────────────────────────────┤
│ PORT                 │ 3000                            │
│ NEO_WORKER_SECRET    │ neo-prod-ТВОЯТА-ПАРОЛА-ТУК      │
└──────────────────────┴─────────────────────────────────┘
```

### 2.4 Create Web Service
- Кликни **Create Web Service**
- Изчакай build-а (3-5 минути първия път)
- Когато е готово, ще видиш зелен статус и URL

### 2.5 Запиши URL-а
Ще получиш URL като:
```
https://neo-worker-abc123.onrender.com
```
**Запиши го - ще го ползваш в Supabase!**

### 2.6 Тествай че работи
Отвори в браузър:
```
https://neo-worker-abc123.onrender.com/health
```

Трябва да видиш:
```json
{
  "status": "ok",
  "uptime": 123,
  "memory": "45 MB"
}
```

---

## Стъпка 3: Supabase Secrets

### 3.1 Отвори Supabase Dashboard
- https://supabase.com/dashboard
- Избери твоя проект

### 3.2 Добави Secrets
1. **Settings** (зъбно колело отляво)
2. **Edge Functions**
3. **Secrets** tab
4. **Add secret**

Добави тези два:

```
┌─────────────────────────────────────────────────────────────┐
│ Secret 1:                                                   │
│ ─────────                                                   │
│ Name:  NEO_WORKER_URL                                       │
│ Value: https://neo-worker-abc123.onrender.com               │
│        (твоя Render URL)                                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Secret 2:                                                   │
│ ─────────                                                   │
│ Name:  NEO_WORKER_SECRET                                    │
│ Value: neo-prod-ТВОЯТА-ПАРОЛА-ТУК                           │
│        (същата като в Render!)                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Стъпка 4: Deploy execute-action

### Вариант А: През Supabase CLI

```bash
# В директорията на твоя Supabase проект
cd supabase/functions

# Копирай новата функция
mkdir -p execute-action
cp /path/to/neo-worker-v2/supabase/functions/execute-action/index.ts execute-action/index.ts

# Deploy
supabase functions deploy execute-action
```

### Вариант Б: През Dashboard (ако нямаш CLI)

1. Supabase Dashboard → **Edge Functions**
2. Намери `execute-action` (или създай нова)
3. **Edit** → Paste кода от `index.ts`
4. **Deploy**

---

## Стъпка 5: Тестване

### 5.1 Тествай Worker директно

```bash
# Health check (без auth)
curl https://neo-worker-abc123.onrender.com/health

# Command (с auth)
curl -X POST https://neo-worker-abc123.onrender.com/command \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer neo-prod-ТВОЯТА-ПАРОЛА" \
  -d '{"action": "open", "url": "https://google.com"}'

# Look
curl -X POST https://neo-worker-abc123.onrender.com/command \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer neo-prod-ТВОЯТА-ПАРОЛА" \
  -d '{"action": "look"}'
```

### 5.2 Тествай през execute-action

```bash
curl -X POST https://YOUR-PROJECT.supabase.co/functions/v1/execute-action \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR-ANON-KEY" \
  -d '{
    "command": "open",
    "url": "https://booking.com"
  }'
```

---

# 🔄 КАК РАБОТИ СИСТЕМАТА

```
┌─────────────────────────────────────────────────────────────────┐
│                        КЛИЕНТ                                    │
│            "Провери дали има свободни стаи"                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     NEO AGENT CORE                               │
│                                                                  │
│  Разбира: "Трябва да проверя наличност"                          │
│  Вика: execute-action с команди                                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EXECUTE-ACTION                                │
│                 (Supabase Edge Function)                         │
│                                                                  │
│  1. Получава: { command: "open", url: "hotel.bg" }               │
│  2. Добавя Authorization header                                  │
│  3. Праща към NEO Worker                                         │
│  4. Връща резултата                                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTPS + Bearer Token
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      NEO WORKER                                  │
│                    (Render Server)                               │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                 PERSISTENT BROWSER                         │  │
│  │                                                            │  │
│  │  "open hotel.bg"                                           │  │
│  │  → Отваря Chromium, зарежда сайта                          │  │
│  │  → Връща: "Готово, title: Hotel Bulgaria"                  │  │
│  │                                                            │  │
│  │  "look"                                                    │  │
│  │  → Сканира DOM                                             │  │
│  │  → Връща: бутони, полета, цени, стаи                       │  │
│  │                                                            │  │
│  │  "click Резервирай"                                        │  │
│  │  → Кликва върху бутона                                     │  │
│  │  → Връща: "Кликнах, отвори се форма"                       │  │
│  │                                                            │  │
│  │  "fill email ivan@mail.bg"                                 │  │
│  │  → Попълва полето                                          │  │
│  │  → Връща: "Попълних email"                                 │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

# 💡 КАК НЕО ЩЕ ПОЛЗВА ТОВА

## Пример 1: Проверка на наличност

```javascript
// В neo-agent-core, когато засечеш intent = "check_availability"

async function checkHotelAvailability(hotelUrl) {
  // 1. Отвори сайта
  await fetch("/functions/v1/execute-action", {
    method: "POST",
    body: JSON.stringify({ command: "open", url: hotelUrl })
  });

  // 2. Виж какво има
  const lookResponse = await fetch("/functions/v1/execute-action", {
    method: "POST",
    body: JSON.stringify({ command: "look" })
  });
  const result = await lookResponse.json();

  // 3. Върни на клиента
  if (result.data?.prices?.length > 0) {
    return `Има налични стаи! Цени: ${result.data.prices.join(", ")}`;
  } else {
    return "Не виждам налични стаи в момента.";
  }
}
```

## Пример 2: Пълна резервация

```javascript
async function makeReservation(hotelUrl, customerData) {
  // 1. Отвори
  await callAction({ command: "open", url: hotelUrl });
  
  // 2. Кликни на "Резервирай" 
  await callAction({ command: "click", target: "Резервирай" });
  
  // 3. Попълни формата
  await callAction({ command: "fill", target: "name", value: customerData.name });
  await callAction({ command: "fill", target: "email", value: customerData.email });
  await callAction({ command: "fill", target: "phone", value: customerData.phone });
  
  // 4. Виж какво има преди submit
  const preview = await callAction({ command: "look" });
  
  // 5. Submit
  const result = await callAction({ command: "submit" });
  
  return result.data?.possibleSuccess 
    ? "Резервацията е направена успешно!"
    : "Изпратих формата, моля проверете имейла си.";
}
```

---

# ⚠️ ВАЖНИ НЕЩА

## Render Free Plan
- Сървърът "заспива" след 15 мин без заявки
- Първата заявка след sleep = ~30 сек
- **Решение:** Starter план ($7/мес) или cron ping

## Сигурност
- **НИКОГА** не споделяй `NEO_WORKER_SECRET`
- Използвай различни пароли за dev/production
- Worker-ът приема заявки САМО с правилен Bearer token

## Производителност
- Worker-ът държи браузъра отворен = бързи отговори
- Първа заявка (open) = ~2-3 сек
- Следващи заявки (look, click) = ~0.3-1 сек

---

# ✅ CHECKLIST

```
RENDER:
[ ] GitHub repo създадено
[ ] Код push-нат
[ ] Web Service създаден
[ ] ENV: PORT=3000 добавен
[ ] ENV: NEO_WORKER_SECRET добавен
[ ] Deploy успешен
[ ] /health отговаря

SUPABASE:
[ ] Secret: NEO_WORKER_URL добавен
[ ] Secret: NEO_WORKER_SECRET добавен (същата парола!)
[ ] execute-action deployed

ТЕСТ:
[ ] Worker /health работи
[ ] Worker /command с auth работи
[ ] execute-action работи
[ ] End-to-end тест минава
```

---

# 🆘 ПРОБЛЕМИ?

## "Unauthorized" грешка
→ Провери дали `NEO_WORKER_SECRET` е **еднаква** в Render и Supabase

## "Worker не отговаря"
→ Провери Render logs
→ Може да е заспал (free plan)

## "Timeout при open"
→ Сайтът е бавен или блокира автоматизация
→ Увеличи timeout в worker.ts

## Render показва грешки при build
→ Провери дали всички файлове са качени
→ Провери package.json

---

Готов си! 🎉
